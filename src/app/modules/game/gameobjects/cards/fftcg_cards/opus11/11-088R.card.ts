import {Scene} from 'phaser';
import FFTCGForward from '../../card_forward';

export default class Elena11 extends FFTCGForward {
    protected _serialNumber = '11-088R';

    constructor(scene: Scene) {
        super(scene);
    }
}