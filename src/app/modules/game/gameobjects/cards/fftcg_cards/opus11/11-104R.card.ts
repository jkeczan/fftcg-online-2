import {Scene} from 'phaser';
import FFTCGForward from '../../card_forward';

export default class Rude11 extends FFTCGForward {
    protected _serialNumber = '11-104R';

    constructor(scene: Scene) {
        super(scene);
    }
}