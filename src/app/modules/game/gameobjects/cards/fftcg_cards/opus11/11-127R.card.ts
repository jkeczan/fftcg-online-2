import {Scene} from 'phaser';
import FFTCGForward from '../../card_forward';

export default class Kadaj11 extends FFTCGForward {
    protected _serialNumber = '11-127R';

    constructor(scene: Scene) {
        super(scene);
    }
}