import {Scene} from 'phaser';
import FFTCGForward from '../../card_forward';

export default class Loz11 extends FFTCGForward {
    protected _serialNumber = '11-142C';

    constructor(scene: Scene) {
        super(scene);
    }
}