import {Scene} from 'phaser';
import FFTCGForward from '../../card_forward';

export default class PresidentShinra extends FFTCGForward {
    protected _serialNumber = '15-136S';

    constructor(scene: Scene) {
        super(scene);
    }
}